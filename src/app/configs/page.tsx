"use client";
import { Typography } from "antd";
const { Title } = Typography;
// const baseEndpoint = process.env.NEXT_PUBLIC_BASEURL_API;

export default function ConfigsPage() {
  return (
    <div style={{ padding: 20 }}>
      <Title level={2}>การจัดการ</Title>
    </div>
  );
}
